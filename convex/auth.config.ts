// https://refined-panda-86.clerk.accounts.dev

const authConfig = {
  providers: [
    {
      domain: "https://refined-panda-86.clerk.accounts.dev",
      applicationID: "convex",
    },
  ],
};
export default authConfig;
